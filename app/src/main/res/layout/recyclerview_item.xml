<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    >

    <TextView
        android:id="@+id/textView"
        android:layout_marginBottom="8dp"
        android:paddingStart="@dimen/layout_spacing_16x"
        android:paddingTop="@dimen/layout_spacing_8x"
        android:paddingBottom="@dimen/layout_spacing_8x"
        android:textSize="@dimen/text_size_16x"
        tools:text="@string/recycler_view_item"
        android:textColor="@color/recyclerview_item_text"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/recyclerview_item_bg"
        android:layout_margin="@dimen/layout_spacing_8x"
        />

</LinearLayout>